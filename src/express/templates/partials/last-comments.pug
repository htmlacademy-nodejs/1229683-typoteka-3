section.main-page__last.last
  h2.last__name
    | Последние комментарии&nbsp;
    span.last__icon.last__icon--cloud
  ul.last__list
    each item in latestComments
      li.last__list-item
        img.last__list-image(src=item.userAvatar, width='20', height='20', alt='Аватар пользователя')
        b.last__list-name #{item.userName}
        a.last__list-link(href='#')
          | #{item.text}
